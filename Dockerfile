# Dockerfile placeholder — implement after core pipeline is stable.
# Intended: Python 3.11 slim image, install requirements, expose dashboard port.

# FROM python:3.11-slim
# WORKDIR /app
# COPY requirements.txt .
# RUN pip install --no-cache-dir -r requirements.txt
# COPY . .
# CMD ["python", "dashboard/app.py"]
