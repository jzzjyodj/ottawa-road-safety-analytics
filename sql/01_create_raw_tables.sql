-- Create the raw collisions table from the City of Ottawa CSV.
-- TODO: replace placeholder path and adjust column definitions after schema inspection.

-- CREATE OR REPLACE TABLE raw_collisions AS
--     SELECT * FROM read_csv_auto('data/raw/traffic_collisions.csv', header=true);
