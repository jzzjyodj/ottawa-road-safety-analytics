-- Assertions against raw_collisions: row counts, null rates, date range.
-- TODO: fill in expected column names after Day 1–2 schema inspection.

-- SELECT COUNT(*) AS total_rows FROM raw_collisions;
-- SELECT COUNT(*) FILTER (WHERE collision_date IS NULL) AS null_dates FROM raw_collisions;
